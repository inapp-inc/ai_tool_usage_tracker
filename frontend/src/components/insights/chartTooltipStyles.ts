import type { CSSProperties } from "react";

/** Recharts default tooltip panel — black background, white text. */
export const insightsChartTooltipStyle: CSSProperties = {
  backgroundColor: "#000",
  color: "#fff",
  border: "none",
  borderRadius: 8,
  fontSize: 12,
  fontWeight: 700,
};

export const insightsChartTooltipLabelStyle: CSSProperties = {
  color: "#fff",
  fontWeight: 700,
};

export const insightsChartTooltipItemStyle: CSSProperties = {
  color: "#fff",
  fontWeight: 700,
};
