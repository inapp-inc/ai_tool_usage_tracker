# Local dev (npm run dev): leave unset — app served at / with API proxy /api/v1.
# Do not set VITE_BASE_PATH here for development.

# Production build (vite build / Docker frontend image) — see .env.production
