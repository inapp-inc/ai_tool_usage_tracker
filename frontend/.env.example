# Leave VITE_API_BASE_URL unset for local dev — Vite proxies /api to the backend (no CORS).
# Uncomment when the frontend cannot use the dev proxy (e.g. Docker frontend profile).
# VITE_API_BASE_URL=http://localhost:8000/api/v1
