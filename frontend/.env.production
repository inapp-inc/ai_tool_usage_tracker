# Used automatically by `vite build` / Docker production frontend image.
# Local `npm run dev` does NOT load this file — dev uses base `/` and `/api/v1` proxy.

VITE_BASE_PATH=/aitool/
VITE_API_BASE_URL=/aitool/api/v1
